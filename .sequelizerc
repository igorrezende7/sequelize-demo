const {resolve} = require('path')


module.exports = {
  "config": resolve(__dirname, "sequelize.js"),
  "models-path":resolve(__dirname,"Models"),
  "migrations-path":resolve(__dirname, "config", "migrations")
}
